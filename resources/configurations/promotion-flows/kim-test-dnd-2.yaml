apiVersion: codefresh.io/v1beta1
kind: PromotionFlow
metadata:
  name: kim-test-dnd-2
  annotations:
    description: ""
spec:
  triggerEnvironment: dev
  steps:
    - environment: qa
      dependsOn:
        - dev
      policy:
        preAction: pre-action
