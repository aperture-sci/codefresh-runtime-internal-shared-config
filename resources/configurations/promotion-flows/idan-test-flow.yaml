apiVersion: codefresh.io/v1beta1
kind: PromotionFlow2
metadata:
  name: idan-test-flow
  annotations:
    description: ""
spec:
  triggerEnvironment: dev
  steps:
    - environment: production
      dependsOn:
        - dev
      policy:
        action: commit
