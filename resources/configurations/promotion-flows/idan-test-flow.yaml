apiVersion: codefresh.io/v1beta1
kind: PromotionFlow
metadata:
  name: idan-test-flow
  annotations:
    description: ""
spec:
  triggerEnvironment: dev
  steps1:
    - environment: production
      dependsOn:
        - dev
      policy:
        action: commit
