apiVersion: codefresh.io/v1beta1
kind: PromotionPolicy
metadata:
  name: non-prod-default
spec:
  priority: 10
  selector:
    targetEnvironment:
      types:
        - NON_PROD
  policy:
    action: commit
    preAction: pre-action
